name: ci_deploiement_API_pythonanywhere_new

# Workflow declenché lors d'un push sur branche main & manuellement sur Github
on:
  push:
    branches:
    - main
  workflow_dispatch:

# 2 jobs dans le workflow : build & deploy
jobs:

  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: checkout
      uses: actions/checkout@v4

    - name: Deploy Pythonanywhere
      run: |
        ssh -o StrictHostKeyChecking=no ${{ secrets.USERNAME }}@www.eu.pythonanywhere.com ls

    - name: Print debugging information
      run: |
        echo "Python Version: $(python --version)"
        echo "Working Directory: $(pwd)"
        echo "Contents of Working Directory: $(ls -l)"
  